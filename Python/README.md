# Parrot Refactoring Kata

## Setting up your environment

You will need `virtualenv` and `virtualenvwrapper`.  Otherwise, if you have a Python installation on your machine, 
you can install the dependencies directly

```bash
mkvirtualenv parrot
pip install pytest
```

## Running the tests

```bash
pytest test_parrot.py
```
